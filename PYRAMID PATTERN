#include <stdio.h>

int main()
{
  int r,s,n,i=1;
  printf("enter n:",n);
  scanf("%d",&n);
  for(r=1;r<=n;r++)
  {
      for(s=1;s<=n-r;s++)
      {
          printf("\t");
      }
      for(i=1;i<=r;i++)
      {
          printf("%d\t\t",i);
      }
       printf("\n");
  }
    return 0;
}
