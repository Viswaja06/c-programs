#include <stdio.h>
int main()
{
    int num,i,a;
    printf("enter a number:",num);
    scanf("%d",&num);
    for(i=num;i!=0;i=i/10)
    {
        a=i%10;
        printf("%d",a);
    }
    return 0;
}
